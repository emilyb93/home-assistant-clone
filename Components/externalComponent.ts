// creates a new component class with all the information provided by an external component on the endpoint
// /api/registerService

// const component = new ExternalComponent()

// This overrides consumeEvent with an axios request to send the event (might use sockets but need to spike how this works in k8s)
